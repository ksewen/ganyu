server:
  port: 8087
  error:
    whitelabel:
      enabled: false

spring:
  application:
    name: ganyu-service
  messages:
    encoding: UTF-8
    basename: validation-messages
  datasource:
    url: jdbc:mysql://localhost:3306/ganyu?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&nullCatalogMeansCurrent=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      auto-commit: true
      minimum-idle: 5
      idle-timeout: 60000
      connection-timeout: 10000
      max-lifetime: 100000
      maximum-pool-size: 10
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    property-naming-strategy: LOWER_CAMEL_CASE


application:
  security:
    jwt:
      secret-key: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
      expiration: '86400000'
      refresh-token-expiration: '604800000'
      read-header: Authorization
      debug: 'true'


logging:
  level:
    com:
      github:
        ksewen:
          ganyu: TRACE
    org:
      springframework:
        security: TRACE
